<!DOCTYPE html>
<html>
<head>
	<title><%= content_for?(:title) ? content_for(:title) : "Elusive Index" %></title>

	<!-- META DATA -->
	<meta property="fb:app_id" content="124425381097108"/>
	<meta property="og:url" content="http://www.elusiveindex.com"/>
	<meta property="og:title" content="Elusive Index"/>
	<meta property="og:description" content="The only way to score, share, and compare your antlers online."/>
	<meta property="og:image" content="http://elusiveindex.com/FB-Share.png"/>
	<%= csrf_meta_tags %>

	<!-- STYLESHEETS -->
	<%= stylesheet_link_tag "application", :media => "all" %>
	<script src="//cdn.optimizely.com/js/240640551.js"></script>

	<!-- SCRIPTS -->
	<%= javascript_include_tag "application" %>

	<%= yield :head %>
</head>
<body class="<%= "#{controller.controller_name} #{@body_class}".strip %>">

	<%= render "shared/header" %>
	
	<%= yield %>

	<%= render "shared/footer" %>

</body>
</html>
